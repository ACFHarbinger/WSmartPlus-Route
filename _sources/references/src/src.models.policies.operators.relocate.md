# {py:mod}`src.models.policies.operators.relocate`

```{py:module} src.models.policies.operators.relocate
```

```{autodoc2-docstring} src.models.policies.operators.relocate
:allowtitles:
```

## Module Contents

### Functions

````{list-table}
:class: autosummary longtable
:align: left

* - {py:obj}`vectorized_relocate <src.models.policies.operators.relocate.vectorized_relocate>`
  - ```{autodoc2-docstring} src.models.policies.operators.relocate.vectorized_relocate
    :summary:
    ```
````

### API

````{py:function} vectorized_relocate(tours, dist_matrix, max_iterations=200)
:canonical: src.models.policies.operators.relocate.vectorized_relocate

```{autodoc2-docstring} src.models.policies.operators.relocate.vectorized_relocate
```
````
