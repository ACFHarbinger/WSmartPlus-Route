# {py:mod}`src.tracking.hooks.attention_hooks`

```{py:module} src.tracking.hooks.attention_hooks
```

```{autodoc2-docstring} src.tracking.hooks.attention_hooks
:allowtitles:
```

## Module Contents

### Functions

````{list-table}
:class: autosummary longtable
:align: left

* - {py:obj}`add_attention_hooks <src.tracking.hooks.attention_hooks.add_attention_hooks>`
  - ```{autodoc2-docstring} src.tracking.hooks.attention_hooks.add_attention_hooks
    :summary:
    ```
````

### API

````{py:function} add_attention_hooks(model_module: torch.nn.Module) -> typing.Dict[str, typing.Any]
:canonical: src.tracking.hooks.attention_hooks.add_attention_hooks

```{autodoc2-docstring} src.tracking.hooks.attention_hooks.add_attention_hooks
```
````
