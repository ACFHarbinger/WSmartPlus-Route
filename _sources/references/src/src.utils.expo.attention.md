# {py:mod}`src.utils.expo.attention`

```{py:module} src.utils.expo.attention
```

```{autodoc2-docstring} src.utils.expo.attention
:allowtitles:
```

## Module Contents

### Functions

````{list-table}
:class: autosummary longtable
:align: left

* - {py:obj}`plot_attention_maps_wrapper <src.utils.expo.attention.plot_attention_maps_wrapper>`
  - ```{autodoc2-docstring} src.utils.expo.attention.plot_attention_maps_wrapper
    :summary:
    ```
````

### API

````{py:function} plot_attention_maps_wrapper(dir_path, attention_dict, model_name, execution_function, layer_idx=0, sample_idx=0, head_idx=0, batch_idx=0, x_labels=None, y_labels=None, **execution_kwargs)
:canonical: src.utils.expo.attention.plot_attention_maps_wrapper

```{autodoc2-docstring} src.utils.expo.attention.plot_attention_maps_wrapper
```
````
