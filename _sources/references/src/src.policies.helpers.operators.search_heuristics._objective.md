# {py:mod}`src.policies.helpers.operators.search_heuristics._objective`

```{py:module} src.policies.helpers.operators.search_heuristics._objective
```

```{autodoc2-docstring} src.policies.helpers.operators.search_heuristics._objective
:allowtitles:
```

## Module Contents

### Functions

````{list-table}
:class: autosummary longtable
:align: left

* - {py:obj}`get_cost <src.policies.helpers.operators.search_heuristics._objective.get_cost>`
  - ```{autodoc2-docstring} src.policies.helpers.operators.search_heuristics._objective.get_cost
    :summary:
    ```
* - {py:obj}`is_better <src.policies.helpers.operators.search_heuristics._objective.is_better>`
  - ```{autodoc2-docstring} src.policies.helpers.operators.search_heuristics._objective.is_better
    :summary:
    ```
````

### API

````{py:function} get_cost(tour: typing.List[int], distance_matrix: numpy.ndarray) -> float
:canonical: src.policies.helpers.operators.search_heuristics._objective.get_cost

```{autodoc2-docstring} src.policies.helpers.operators.search_heuristics._objective.get_cost
```
````

````{py:function} is_better(c1: float, c2: float) -> bool
:canonical: src.policies.helpers.operators.search_heuristics._objective.is_better

```{autodoc2-docstring} src.policies.helpers.operators.search_heuristics._objective.is_better
```
````
