# =============================================================================
# Post-Processing: Fix-and-Optimize
# =============================================================================
# PURPOSE:
#   Select worst routes and solve them exactly via Gurobi sub-MIP.
# =============================================================================

methods:
  - "fix_and_optimize"

fo_free_fraction: 0.30
fo_time_limit: 30.0
seed: 42
