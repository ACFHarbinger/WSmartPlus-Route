# @package _global_

data:
  dataset_size: 128000     # n_data
  data_distributions: ["gamma1"] # data_dist
  filename: "real"         # dataset_name
  graph_sizes: [1]         # f_size (assuming f_size meant focus size or graph size? context implies graph size usually but hpo uses small 1?)

train:
  val_data_size: 1280      # n_val_data
  eval_batch_size: 256     # val_b_size
