# @package _global_

data:
  dataset_size: 1280       # n_data
  data_distributions: ["gamma1"] # data_dists
  graph_sizes: [100]       # size

env:
  distance_method: "gmaps" # dm_method

train:
  val_data_size: 0         # n_val_data
