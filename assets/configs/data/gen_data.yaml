# @package _global_

data:
  dataset_size: 1280       # n_data
  data_distributions: ["gamma1"] # data_dists

  # Dataset specifics
  dataset_type: "train_time" # d_type

  # Generation flags (NOTE: These map to unused legacy args in generate_data.py but keeping for record)
  filename: "time"         # dataset_name

  # Focus graphs
  focus_size: 1280

# Training specific overrides often found in old gen_data
train:
  val_data_size: 128       # n_val_data
