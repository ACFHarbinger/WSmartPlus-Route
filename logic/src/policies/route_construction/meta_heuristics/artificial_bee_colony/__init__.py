r"""Artificial Bee Colony (ABC) algorithm for VRPP.

This module implements the Artificial Bee Colony (ABC) meta-heuristic for
solving the Vehicle Routing Problem with Profits (VRPP).

Attributes:
    ABCSolver: The main solver implementation using the ABC algorithm.
    ABCPolicy: Adapter class for the ABC solver.
    ABCParams: Parameter configuration for the ABC algorithm.

Example:
    >>> from logic.src.policies.route_construction.meta_heuristics.artificial_bee_colony import ABCPolicy
    >>> policy = ABCPolicy()
"""
