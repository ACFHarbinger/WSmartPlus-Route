"""Discrete Firefly Algorithm (FA) for VRPP.

Attributes:
    FASolver: Main solver class for DFA.
    FAPolicy: Policy class for DFA.
    FAParams: Parameter dataclass for DFA.

Example:
    >>> from logic.src.policies.route_construction.meta_heuristics.firefly_algorithm import FAPolicy
    >>> policy = FAPolicy()
"""
