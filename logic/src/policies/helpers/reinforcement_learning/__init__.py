"""Reinforcement Learning Helpers Module.

This module provides helper classes and functions for implementing Reinforcement
Learning (RL) policies, including bandit agents, reward functions, and feature
extractors.

Attributes:
    agents: Sub-package containing RL agent implementations (e.g., bandits).
    reward: Sub-package containing reward calculation logic.
    features: Sub-package containing feature extraction utilities.

Example:
    >>> from logic.src.policies.helpers.reinforcement_learning.agents import BanditAgent
    >>> agent = BanditAgent(...)
"""
