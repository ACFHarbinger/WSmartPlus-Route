"""
WSmart-Route Simulator Package.

This package contains the core logic for the waste collection simulation,
including bin dynamics (physics), routing network analysis, daily execution
pipeline (actions), and data persistence.

Attributes:
    Simulator: Main orchestrator for the simulation environment.
    SimulationState: Dataclass representing the current state of a simulation run.

Example:
    >>> # from logic.src.pipeline.simulations.orchestrator import Simulator
    >>> # sim = Simulator(cfg)
    >>> # results = sim.run()
"""
