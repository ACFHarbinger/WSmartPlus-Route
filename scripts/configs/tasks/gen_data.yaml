# @package _global_

# General
verbose: true
seed: 42
start: 0

# Data Configuration
data:
  n_epochs: 100
  problem: "wcvrp"

# Note: 'dataset_type' must be provided via CLI (e.g. dataset_type=train)
# as it is required by generate_data.py but not set here.
