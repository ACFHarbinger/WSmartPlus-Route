defaults:
  - _self_
  - tasks: train
  - envs: cwcvrp
  - model: am
  - data: train_data


# General Configuration
seed: 42
verbose: true
task: "train"
start: 0
device: "cuda"

# Config Schema Defaults (Moved from flat keys)
# These will be overridden by tasks/train.yaml or other task configs.
train:
  logs_dir: "logs"
  model_weights_path: "model_weights"
  log_step: 10

# Root Config Fields
log_dir: "logs"
output_dir: "assets/model_weights"
wandb_mode: "disabled"
